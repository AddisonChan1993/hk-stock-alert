name: StockAlert
on:
  schedule:
    - cron: '15 1 * * 1-5'  # 🇭🇰 香港時間 09:15
    - cron: '45 4 * * 1-5'  # 🇭🇰 香港時間 12:45
    - cron: '05 8 * * 1-5'  # 🇭🇰 香港時間 16:05
  workflow_dispatch:        # 手動按鈕 (留返嚟測試)
  push:
    branches: [ main ]      # 💡 只要你 Save 檔案，佢就會即刻 Run 一次

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install yfinance pandas requests
      - name: Run Analysis
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python monitor.py
